# Archetype addendum: `solver_compete`

Answer after the universal block.

1. **Submission path** — single file or folder the harness reads; lock `mutableSurface` accordingly.

2. **Timeout per submission** — wall-clock limit aligned with contest rules.

3. **Scoring** — tests passed, weighted score, penalty; extraction from harness stdout.

4. **Hidden tests** — if unknown, metric may be proxy only—flag `needs_harness` or document risk.
