Vendored baseline harness (`run_baseline.sh`, `_log.sh`, `_log.py`, `preview_metrics.py`) copied from **`autoresearch-create/scripts`** / **`autoresearch-mine/vendor/harness`**.

When the upstream harness changes in `autoresearch-create`, refresh this directory so validators stay aligned with miners.
